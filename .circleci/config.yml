version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:14.4.0
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: yarn install
      - run:
          name: Test
          command: yarn test